{
  "name": "whatsapp-sender-cloud",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/prepare.mjs && node scripts/verify-deploy.mjs && node tests/deployment.test.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check src/core.js",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "test:deploy": "node tests/deployment.test.mjs"
  }
}
